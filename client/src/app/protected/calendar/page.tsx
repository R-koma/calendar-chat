import Logout from '@/components/auth/LogoutButton';
import React from 'react';

const calendar = () => {
  return (
    <div>
      <div>calendar</div>
      <Logout />
    </div>
  );
};

export default calendar;
